#!/usr/bin/env stox
// -*- mode: text; -*-

var a = 0;
var temp;

for (var b=1; a < 5000; b = temp + b) {
  print a;
  temp = a;
  a = b;
}

fun fib(n) {
  if (n <= 1) return n;
  return fib(n - 2) + fib(n - 1);
}
print fib(20);
