#!/usr/bin/env stox

class Breakfast {
  cook() {
    print "Fried eggs.";
  }
  serve(who) {
    print "Enjoy, " + who + ".";
  }
}

class Bagel {}
var bagel = Bagel();

print bagel.isReady ? "Bagel is ready" : "... still toasting...";

// Breakfast().cook().serve('Customer');
